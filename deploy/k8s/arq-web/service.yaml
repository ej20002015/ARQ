apiVersion: v1
kind: Service
metadata:
  name: arq-web-svc
  labels:
    app: arq-web
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "arq-web-${ENV_NAME}"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http-web
      protocol: TCP
      name: http
  selector:
    app: arq-web