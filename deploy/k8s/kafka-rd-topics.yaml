## THIS FILE IS AUTO-GENERATED BY THE CODE-GEN SCRIPT. DO NOT EDIT.
## Contains all the refdata entity kafka topics

# ===================================
#           Update Topics           
# ===================================
# - 20 partitions
# - Retain forever
# - 3 replicas and 2 in-sync for strong durability
# ===================================

apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: arq-refdata-updates-currency
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.RefData.Updates.Currency
  partitions: 20
  replicas: 3
  config:
    retention.ms: -1
    min.insync.replicas: 2

---

apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: arq-refdata-updates-user
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.RefData.Updates.User
  partitions: 20
  replicas: 3
  config:
    retention.ms: -1
    min.insync.replicas: 2

---

# ===================================
#           Command Topics
# ===================================
# - 20 partitions (co-partitioned with Update topics so must be the same)
# - Retain for 30 days
# - 2 replicas for some durability
# ===================================
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: arq-refdata-commands-currency
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.RefData.Commands.Currency
  partitions: 20
  replicas: 2
  config:
    retention.ms: 2592000000
    min.insync.replicas: 1

---

apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: arq-refdata-commands-user
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.RefData.Commands.User
  partitions: 20
  replicas: 2
  config:
    retention.ms: 2592000000
    min.insync.replicas: 1
