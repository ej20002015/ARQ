## THIS FILE IS AUTO-GENERATED BY THE CODE-GEN SCRIPT. DO NOT EDIT.
## Contains all the mktdata entity kafka topics

# ===================================
#         Observation Topics
# ===================================
# - 4 partitions
# - Retain authoritative observations for the downstream recovery window
# - Permanent history belongs in the audit projection
# - 3 replicas and 2 in-sync for strong durability
# ===================================

apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: arq-mktdata-observations-fxrate
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.MktData.Observations.FXR
  partitions: 4
  replicas: 3
  config:
    retention.ms: 604800000
    cleanup.policy: delete
    min.insync.replicas: 2

---

apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: arq-mktdata-observations-eqprice
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.MktData.Observations.EQP
  partitions: 4
  replicas: 3
  config:
    retention.ms: 604800000
    cleanup.policy: delete
    min.insync.replicas: 2

---

# ===================================
#           Current Topics
# ===================================
# - 4 partitions, matching the corresponding observation topic
# - Compact current state by the stable market/type/id key
# - Make superseded values eligible for compaction promptly
# - 3 replicas and 2 in-sync for strong durability
# ===================================

apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: arq-mktdata-current-fxrate
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.MktData.Current.FXR
  partitions: 4
  replicas: 3
  config:
    max.compaction.lag.ms: 3600000
    segment.ms: 3600000
    cleanup.policy: compact
    min.insync.replicas: 2

---

apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: arq-mktdata-current-eqprice
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  topicName: ARQ.MktData.Current.EQP
  partitions: 4
  replicas: 3
  config:
    max.compaction.lag.ms: 3600000
    segment.ms: 3600000
    cleanup.policy: compact
    min.insync.replicas: 2
