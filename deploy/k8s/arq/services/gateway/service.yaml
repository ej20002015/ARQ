apiVersion: v1
kind: Service
metadata:
  name: arq-gateway-svc
  labels:
    app: arq-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: http-web
      protocol: TCP
      name: http
  selector:
    app: arq-gateway