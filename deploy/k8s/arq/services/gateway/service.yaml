apiVersion: v1
kind: Service
metadata:
  name: arq-gateway-svc
  labels:
    app: arq-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 8080 # Can't use 80 - that's in use by ServiceLB already
      targetPort: http-web
      protocol: TCP
      name: http
  selector:
    app: arq-gateway