{#
  // codegen-metadata
  // output_path: dotnet/ARQ.NET/src/Core/RD/EntityInterfaces.cs
#}
// THIS FILE IS AUTO-GENERATED BY THE CODE-GEN SCRIPT. DO NOT EDIT.
// Contains interfaces for Reference Data entities.

namespace ARQ.RD;

public interface IEntity { }
public interface IRecord { }
{% for entity in entities %}

public partial class {{ entity.name }} : IEntity {}
public partial class {{ entity.name }}Record : IRecord {}
{% endfor %}

{# public static class Dispatcher
{
    /// <summary>
    /// Translates a string entity name into a strongly-typed visitor call.
    /// Completely agnostic of data access or repositories.
    /// </summary>
    public static TResult Dispatch<TResult>(string entityName, IEntityVisitor<TResult> visitor)
    {
        return entityName.ToLowerInvariant() switch
        {
            {% for entity in entities %}
            "{{ entity.name | lower }}" => visitor.Visit<{{ entity.name }}>(),
            {% endfor %}
            
            _ => throw new ArgumentException($"Unknown RefData entity name [{entityName}] - cannot dispatch to visitor")
        };
    }
} #}