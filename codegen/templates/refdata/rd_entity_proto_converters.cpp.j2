{#
  // codegen-metadata
  // output_path: ARQLib/ARQProtobuf/src/rd_entity_proto_converters.cpp
#}
// THIS FILE IS AUTO-GENERATED BY THE CODE-GEN SCRIPT. DO NOT EDIT.
// Contains functions to convert between RefData entity structs and their Protobuf representations.

#include <ARQProtobuf/rd_entity_proto_converters.h>

namespace ARQ::Proto::RD
{

{% for entity in entities %}
// --- Converters for {{ entity.name }} ---

void toProto( const ARQ::RD::{{ entity.name }}& arqEntity, {{ entity.name }}* const protoEntity )
{
    std::string* uuidBufPtr = protoEntity->mutable_uuid()->mutable_id();
	*uuidBufPtr = arqEntity.uuid.toString();

    {% for member in entity.members %}
    {% set arq_conv_end = ".microsecondsSinceEpoch()" if member.type == "datetime" else ""%}
    {% if member.optional %}
    if( arqEntity.{{ member.name }} )
        protoEntity->set_{{ member.name | snake_case }}( *arqEntity.{{ member.name }}{{ arq_conv_end }} );
    {% else %}
    protoEntity->set_{{ member.name | snake_case }}( arqEntity.{{ member.name }}{{ arq_conv_end }} );
    {% endif %}
    {% endfor %}
}

void toProto( ARQ::RD::{{ entity.name }}&& arqEntity, {{ entity.name }}* const protoEntity )
{
    std::string* uuidBufPtr = protoEntity->mutable_uuid()->mutable_id();
	*uuidBufPtr = arqEntity.uuid.toString();

    {% for member in entity.members %}
    {% set arq_conv_end = ".microsecondsSinceEpoch()" if member.type == "datetime" else ""%}
    {% if member.optional %}
    if( arqEntity.{{ member.name }} )
        protoEntity->set_{{ member.name | snake_case }}( std::move( *arqEntity.{{ member.name }} ){{ arq_conv_end }} );
    {% else %}
    protoEntity->set_{{ member.name | snake_case }}( std::move( arqEntity.{{ member.name }} ) );
    {% endif %}
    {% endfor %}
}

ARQ::RD::{{ entity.name }} fromProto( const {{ entity.name }}& protoEntity )
{
    ARQ::RD::{{ entity.name }} arqEntity;
    arqEntity.uuid = ARQ::ID::UUID::fromString( protoEntity.uuid().id() );
    {% for member in entity.members %}
    {% set arq_conv_start = "Time::DateTime( Time::Microseconds( " if member.type == "datetime" else "" %}
    {% set arq_conv_end = " ) )" if member.type == "datetime" else "" %}
    {% if member.optional %}
    arqEntity.{{ member.name }} = protoEntity.has_{{ member.name | snake_case }}() ? std::optional<{{ types[member.type].cpp }}>( {{ arq_conv_start }}protoEntity.{{ member.name | snake_case }}(){{ arq_conv_end }} ) : std::nullopt;
    {% else %}
    arqEntity.{{ member.name }} = {{ arq_conv_start }}protoEntity.{{ member.name | snake_case }}(){{ arq_conv_end }};
    {% endif %}
    {% endfor %}
    return arqEntity;
}

ARQ::RD::{{ entity.name }} fromProto( {{ entity.name }}&& protoEntity )
{
    ARQ::RD::{{ entity.name }} arqEntity;
    arqEntity.uuid = ARQ::ID::UUID::fromString( protoEntity.uuid().id() );
    {% for member in entity.members %}
    {% set arq_conv_start = "Time::DateTime( Time::Microseconds( " if member.type == "datetime" else "" %}
    {% set arq_conv_end = " ) )" if member.type == "datetime" else "" %}
    {% set proto_member = "std::move( *protoEntity.mutable_" + member.name | snake_case + "() )" if member.type == "string" else "protoEntity." + member.name | snake_case + "()" %}
    {% if member.optional %}
    arqEntity.{{ member.name }} = protoEntity.has_{{ member.name | snake_case }}() ? std::optional<{{ types[member.type].cpp }}>( {{ arq_conv_start }}{{ proto_member }}{{ arq_conv_end }} ) : std::nullopt;
    {% else %}
    arqEntity.{{ member.name }} = {{ arq_conv_start }}{{ proto_member }}{{ arq_conv_end }};
    {% endif %}
    {% endfor %}
    return arqEntity;
}

{% endfor %}
}